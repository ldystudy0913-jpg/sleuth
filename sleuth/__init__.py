"""sleuth — a Python coding agent.

Provides:
* provider-agnostic messaging and streaming
* a tool registry (read/write/edit/bash/...)
* an agentic session loop
* .env-first configuration with optional JSONC overlays
* remote MCP tools, external skills (paths / URLs / S3), SQLite or MySQL storage
"""

__version__ = "0.1.0"
