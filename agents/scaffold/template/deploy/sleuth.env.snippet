# Paste into Sleuth .env (client). Do NOT put __ENV_PREFIX___* secrets here.

# agent:true registers get_agent_card. --skill none writes false (tools on every session).
SLEUTH_MCP_SERVERS={"__SERVER_NAME__":{"type":"remote","url":"http://127.0.0.1:__MCP_PORT__/mcp","agent":__AGENT_TRUE__}}

# COS / S3 shared skills (prefix scan). Uncomment when --skill cos|both.
# SLEUTH_SKILLS_S3=s3://<bucket>/sleuth/skills/

# Legacy (tools only + local files; keep local agent.md + skills paths):
# SLEUTH_MCP_SERVERS={"__SERVER_NAME__":{"type":"remote","url":"http://127.0.0.1:__MCP_PORT__/mcp"}}
# SLEUTH_SKILLS_PATHS=<this-package>/skills
