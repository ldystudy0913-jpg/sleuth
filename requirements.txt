# Core (keep versions in sync with pyproject.toml [project].dependencies)
openai>=1.50
pydantic>=2.5
rich>=13.0
mcp>=1.2
tzdata>=2024.1
boto3>=1.34

# Optional extras — prefer: python -m pip install -e ".[<extra>]"
# mysql   → PyMySQL>=1.1
# server  → starlette>=0.37, uvicorn>=0.30, python-multipart>=0.0.9
# files   → pypdf>=4.0, pypdfium2>=4.30, openpyxl>=3.1, python-docx>=1.1
# ocr     → rapidocr-onnxruntime>=1.3
# memory  → psycopg2-binary>=2.9
# all     → mysql + server + files + memory (not log-trace)
#
# OpenGauss (memory) must be installed from this repo with the same interpreter:
#   python -m pip install -e ".[memory]"
# Do not `pip install sleuth[memory]` from a corporate index (package is not published).

# Optional: 北斗 log_trace (intranet PyPI only; do not uncomment for public pip)
#   pip install "log-trace[mysql]>=4.10"
# then SLEUTH_LOG_TRACE=1 and SLEUTH_LOG_TRACE_CONFIG=log_trace.toml
# sqlglot / dynaconf / loguru / CMBSM come with that package — do not add them here.
# log-trace[mysql]>=4.10
