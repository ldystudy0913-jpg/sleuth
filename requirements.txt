# Core: OpenAI SDK talks to OpenAI and any OpenAI-compatible gateway
# (OpenRouter, Groq, local servers, ...).
openai>=1.50

# Tool parameter validation + JSON schema generation.
pydantic>=2.5

# Optional but recommended: nicer terminal output.
rich>=13.0

# Tencent COS / S3-compatible session file mailbox (also used by remote Skills).
boto3>=1.34
