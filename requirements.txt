# Core: OpenAI SDK talks to OpenAI and any OpenAI-compatible gateway
# (OpenRouter, Groq, local servers, ...).
openai>=1.50

# Tool parameter validation + JSON schema generation.
pydantic>=2.5

# Optional but recommended: nicer terminal output.
rich>=13.0

# Optional OpenGauss driver: from this repo, same interpreter as the server
#   python -m pip install -e ".[memory]"
# Do not `pip install sleuth[memory]` from a corporate index (package is not published).
# Optional: pip install sleuth[mysql]   →  PyMySQL
